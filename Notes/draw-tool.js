// Tools that listen to user interaction events (pointer events)

1) draw
// mouse/pointer events for Draw
down()
  isDown = true
  // create Graphics and store in class
  // store (x,y) in path var
up()
  isDown = false
  // clear path var
  // Convert to cached bitMap or image
  // delay 1 sec then call character recognition on canvas image
move()
  // Store (x,y) in path var
  // draw Polygon using path

